---
name: r-development
description: Apply ricochet-rs R formatting, linting, and validation conventions when changing R packages, scripts, Shiny applications, or R tests.
---

# R development

Use repository `just` commands when provided.
Format R files with Air through `just format`.
Apply Jarl fixes and linting through `just fix`.
Run the repository's relevant R tests after formatting and linting.
